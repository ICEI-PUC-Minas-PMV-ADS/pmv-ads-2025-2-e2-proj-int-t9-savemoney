@model SaveMoney.Models.ViewModels.ProjecaoViewModel

@{
    ViewData["Title"] = "Projeção Financeira";
    int mesesSelecionados = ViewBag.MesesSelecionados ?? 6;
}

@section css {
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link rel="stylesheet" href="~/css/projecao.css" asp-append-version="true" />
}

<main class="projecao-page-main fade-in">

    <!-- Page Header -->
    <header class="page-header">
        <div class="header-content">
            <div class="header-title-group">
                <h1 class="page-title">
                    <span class="material-symbols-outlined page-icon" aria-hidden="true">trending_up</span>
                    Projeção Financeira
                </h1>
                <p class="page-subtitle">Análise de tendências baseada no seu comportamento recorrente</p>
            </div>
        </div>

        <div class="header-actions">
            <button class="btn-action btn-info"
                    id="btnToggleInfo"
                    aria-expanded="false"
                    aria-controls="infoSection">
                <span class="material-symbols-outlined" aria-hidden="true">school</span>
                <span id="btnInfoText">Entenda o Cálculo</span>
            </button>

            <form method="get" class="select-container">
                <label for="mesesSelect" class="visually-hidden">Selecionar meses de projeção</label>
                <select name="meses"
                        id="mesesSelect"
                        class="select-input"
                        onchange="this.form.submit()"
                        aria-label="Quantidade de meses para projeção">
                    @for (int i = 1; i <= 12; i++)
                    {
                        <option value="@i" selected="@(i == mesesSelecionados)">
                            Projetar @i @(i == 1 ? "Mês" : "Meses")
                        </option>
                    }
                </select>
                <span class="material-symbols-outlined select-icon" aria-hidden="true">expand_more</span>
            </form>
        </div>
    </header>

    <!-- KPI Cards -->
    <section class="kpi-section" aria-label="Indicadores financeiros">
        <div class="kpi-grid">

            <!-- Saldo Atual -->
            <article class="kpi-card glass-panel">
                <div class="kpi-content">
                    <div class="kpi-info">
                        <span class="kpi-label">Saldo Hoje</span>
                        <h2 class="kpi-value">@Model.SaldoAtual.ToString("C")</h2>
                        <span class="kpi-description">Disponível em conta</span>
                    </div>
                    <div class="kpi-icon-box kpi-icon-primary">
                        <span class="material-symbols-outlined" aria-hidden="true">account_balance_wallet</span>
                    </div>
                </div>
            </article>

            <!-- Fluxo Mensal -->
            <article class="kpi-card glass-panel">
                <div class="kpi-content">
                    <div class="kpi-info">
                        <span class="kpi-label">Fluxo Mensal Recorrente</span>
                        <h2 class="kpi-value @(Model.FluxoMensal >= 0 ? "value-positive" : "value-negative")">
                            @((Model.FluxoMensal > 0 ? "+" : "") + Model.FluxoMensal.ToString("C"))
                        </h2>
                        <span class="kpi-description">Quanto sobra (ou falta) todo mês</span>
                    </div>
                    <div class="kpi-icon-box @(Model.FluxoMensal >= 0 ? "kpi-icon-success" : "kpi-icon-danger")">
                        <span class="material-symbols-outlined" aria-hidden="true">
                            @(Model.FluxoMensal >= 0 ? "savings" : "trending_down")
                        </span>
                    </div>
                </div>
            </article>

            <!-- Saldo Projetado -->
            <article class="kpi-card glass-panel kpi-card-featured">
                <div class="kpi-glow"></div>
                <div class="kpi-content">
                    <div class="kpi-info">
                        <span class="kpi-label">Estimativa em @mesesSelecionados @(mesesSelecionados == 1 ? "Mês" : "Meses")</span>
                        <h2 class="kpi-value @(Model.SaldoProjetado6Meses >= 0 ? "value-positive" : "value-negative")">
                            @Model.SaldoProjetado6Meses.ToString("C")
                        </h2>
                        <span class="kpi-description">Previsão matemática</span>
                    </div>
                    <div class="kpi-icon-box kpi-icon-accent">
                        <span class="material-symbols-outlined" aria-hidden="true">auto_awesome</span>
                    </div>
                </div>
            </article>

        </div>
    </section>

    <!-- Info Alert -->
    <div class="alert alert-info">
        <span class="material-symbols-outlined alert-icon" aria-hidden="true">info</span>
        <div class="alert-content">
            <h3 class="alert-title">Por que o gráfico é uma linha reta?</h3>
            <p class="alert-text">
                Esta projeção assume que suas receitas e despesas recorrentes (o Fluxo Mensal acima)
                se repetirão exatamente iguais no futuro. Gastos variáveis imprevisíveis não entram nesta conta.
            </p>
        </div>
    </div>

    <!-- Gráfico -->
    <section class="chart-section glass-panel">
        <header class="chart-header">
            <h2 class="chart-title">
                <span class="material-symbols-outlined" aria-hidden="true">show_chart</span>
                Linha do Tempo Patrimonial
            </h2>
        </header>
        <div class="chart-wrapper">
            <canvas id="meuGraficoCanvas"
                    role="img"
                    aria-label="Gráfico de projeção financeira ao longo do tempo"></canvas>
        </div>
    </section>

    <!-- Seção Educativa (Toggle) -->
    <section id="infoSection" class="info-section" style="display: none;" aria-labelledby="infoSectionTitle">

        <!-- Header -->
        <div class="info-header">
            <h2 id="infoSectionTitle" class="info-title">O que é Projeção Financeira?</h2>
            <p class="info-intro">
                A projeção financeira é uma estimativa de como estarão suas finanças no futuro.
                Ela utiliza seu <strong>saldo atual</strong> e seus hábitos de consumo (receitas e despesas <strong>recorrentes</strong>)
                para prever se você terá dinheiro sobrando ou faltando nos próximos meses.
            </p>
        </div>

        <!-- Passo a Passo -->
        <div class="passos-container">
            <h3 class="passos-title">Como o cálculo é feito?</h3>
            <p class="passos-intro">O sistema ignora gastos pontuais (como um jantar caro) e foca no que se repete. Imagine o seguinte cenário:</p>

            <div class="passos-grid">
                <div class="passo-card glass-panel">
                    <div class="passo-number">1</div>
                    <h4 class="passo-title">Saldo Inicial</h4>
                    <p class="passo-text">Você começa com o dinheiro que tem hoje na conta. Ex: <strong>R$ 1.000</strong>.</p>
                </div>
                <div class="passo-card glass-panel">
                    <div class="passo-number">2</div>
                    <h4 class="passo-title">O que entra todo mês</h4>
                    <p class="passo-text">Seu salário e rendas fixas. Ex: <strong>+ R$ 3.000</strong>.</p>
                </div>
                <div class="passo-card glass-panel">
                    <div class="passo-number">3</div>
                    <h4 class="passo-title">O que sai todo mês</h4>
                    <p class="passo-text">Aluguel, internet, contas fixas. Ex: <strong>- R$ 2.000</strong>.</p>
                </div>
            </div>

            <div class="formula-box glass-panel">
                <p class="formula-item"><strong>A Lógica:</strong> Todo mês sobra R$ 1.000 (3.000 - 2.000).</p>
                <p class="formula-item"><strong>Em 6 meses:</strong> R$ 1.000 (hoje) + (6 × R$ 1.000 de sobra) = <strong>R$ 7.000</strong>.</p>
                <p class="formula-item"><strong>Conclusão:</strong> Você consegue visualizar seu crescimento patrimonial.</p>
            </div>
        </div>

        <!-- FAQ Accordion -->
        <div class="faq-container">
            <h3 class="faq-title">Dúvidas Comuns</h3>

            <div class="accordion">
                <div class="accordion-item">
                    <button class="accordion-button"
                            type="button"
                            aria-expanded="false"
                            aria-controls="faq-1">
                        <span class="accordion-question">Por que minha projeção deu errada?</span>
                        <span class="accordion-icon material-symbols-outlined" aria-hidden="true">expand_more</span>
                    </button>
                    <div id="faq-1" class="accordion-content">
                        <p>Verifique se você marcou suas Receitas e Despesas fixas (como salário e aluguel) com a opção <strong>"É Recorrente?"</strong> e se a data de início delas engloba o período atual.</p>
                    </div>
                </div>

                <div class="accordion-item">
                    <button class="accordion-button"
                            type="button"
                            aria-expanded="false"
                            aria-controls="faq-2">
                        <span class="accordion-question">Gastos únicos afetam a projeção?</span>
                        <span class="accordion-icon material-symbols-outlined" aria-hidden="true">expand_more</span>
                    </button>
                    <div id="faq-2" class="accordion-content">
                        <p>Não. A projeção ignora gastos únicos (não recorrentes) para focar na tendência de longo prazo do seu fluxo de caixa.</p>
                    </div>
                </div>

                <div class="accordion-item">
                    <button class="accordion-button"
                            type="button"
                            aria-expanded="false"
                            aria-controls="faq-3">
                        <span class="accordion-question">O que acontece se a projeção for negativa?</span>
                        <span class="accordion-icon material-symbols-outlined" aria-hidden="true">expand_more</span>
                    </button>
                    <div id="faq-3" class="accordion-content">
                        <p>Significa que suas despesas fixas são maiores que suas receitas fixas. Seu saldo vai diminuir mês a mês até ficar vermelho. É um alerta para cortar gastos fixos!</p>
                    </div>
                </div>
            </div>
        </div>

    </section>

</main>

@section Scripts {
    <script>
        window.saveMoneyDados = {
            labels: @Html.Raw(Json.Serialize(Model.Meses)),
            values: @Html.Raw(Json.Serialize(Model.Saldos))
        };
    </script>
    <script src="~/js/projecao.js" asp-append-version="true"></script>
}