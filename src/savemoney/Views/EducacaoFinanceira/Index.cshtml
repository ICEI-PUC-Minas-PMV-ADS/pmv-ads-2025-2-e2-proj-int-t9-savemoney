<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exemplo Funcional - Educação Financeira</title>

    <!-- 1. INCLUINDO BOOTSTRAP VIA CDN -->
    <!-- Puxamos o CSS diretamente da internet para garantir que o estilo sempre funcione. -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        /* Estilos personalizados para deixar os cards mais bonitos */
        body {
            background-color: #f0f2f5;
            font-family: 'Poppins', sans-serif;
        }
        .news-card {
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
            border: none;
            border-radius: 12px;
        }
        .news-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
        }
        .card-img-top {
            border-top-left-radius: 12px;
            border-top-right-radius: 12px;
            height: 200px;
            object-fit: cover;
        }
        .card-title {
            font-weight: 600;
        }
        .card-text {
            color: #6c757d;
            font-size: 0.9rem;
        }
        a.card-link {
            text-decoration: none;
            color: inherit;
        }
        .spinner-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 50vh;
        }
    </style>
</head>
<body>

    <div class="container my-5">
        <h1 class="text-center mb-4 fw-bold">Últimas Notícias Financeiras</h1>

        <div id="noticias-container" class="row g-4">
            <div class="spinner-container">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden">Carregando...</span>
                </div>
            </div>
        </div>
    </div>

    <!-- Incluindo o JavaScript do Bootstrap no final do body para melhor performance -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function() {

            async function carregarNoticias() {
                const container = document.getElementById('noticias-container');
                
                try {
                    // --- CORREÇÃO IMPORTANTE PARA O ERRO DE SSL ---
                    //
                    // O erro "ERR_SSL_PROTOCOL_ERROR" geralmente acontece porque o navegador não confia
                    // no certificado de desenvolvimento local do seu servidor ASP.NET.
                    //
                    const baseUrl = 'http://localhost:5248'; // <-- SUBSTITUA ESTA URL PELA SUA!
                    const apiUrl = `${baseUrl}/EducacaoFinanceira/GetNoticias`;
                    
                    const response = await fetch(apiUrl);

                    if (!response.ok) {
                        throw new Error(`Erro na API: ${response.statusText} (Status: ${response.status})`);
                    }

                    const dadosCompletos = await response.json();
                    
                    if (!dadosCompletos || !dadosCompletos.articles) {
                        throw new Error("A resposta da API não continha a propriedade 'articles' esperada.");
                    }
                    
                    const listaDeNoticias = dadosCompletos.articles; 

                    container.innerHTML = '';

                    // Se a lista de notícias estiver vazia, mostramos uma mensagem
                    if (listaDeNoticias.length === 0) {
                        container.innerHTML = '<div class="alert alert-info">Nenhuma notícia encontrada no momento.</div>';
                        return;
                    }

                    listaDeNoticias.forEach(noticia => {
                        const imageUrl = noticia.urlToImage ? noticia.urlToImage : 'https://placehold.co/600x400/6a0dad/white?text=Noticia';
                        
                        const cardHTML = `
                            <div class="col-md-6 col-lg-4">
                                <a href="${noticia.url}" target="_blank" rel="noopener noreferrer" class="card-link">
                                    <div class="card news-card h-100">
                                        <img src="${imageUrl}" class="card-img-top" alt="${noticia.title}">
                                        <div class="card-body d-flex flex-column">
                                            <h5 class="card-title">${noticia.title}</h5>
                                            <p class="card-text flex-grow-1">${noticia.description || 'Clique para ler mais na fonte original.'}</p>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        `;
                        container.innerHTML += cardHTML;
                    });

                } catch (error) {
                    console.error("Falha ao carregar notícias:", error);
                    container.innerHTML = `<div class="alert alert-danger">Desculpe, não foi possível carregar as notícias. Verifique a consola (F12) para mais detalhes do erro. <br><strong>Causa Comum:</strong> A URL base (<code>baseUrl</code>) no ficheiro JavaScript pode estar incorreta. Verifique se o protocolo (https) e a porta correspondem exatamente ao que o seu servidor backend está a usar.</div>`;
                }
            }

            carregarNoticias();
        });
    </script>

</body>
</html>

