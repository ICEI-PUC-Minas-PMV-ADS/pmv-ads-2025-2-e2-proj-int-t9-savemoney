@model savemoney.Models.MetaFinanceira

@{
    ViewData["Title"] = "Nova Meta Financeira";
}

@section css {
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link rel="stylesheet" href="~/css/metasFinanceiras.css" asp-append-version="true" />
}

<main class="metas-page-main fade-in">

    <!-- Page Header -->
    <header class="page-header-simple">
        <h1 class="page-title">Nova Meta Financeira</h1>
        <p class="page-subtitle">Defina um objetivo financeiro e acompanhe seu progresso</p>
    </header>

    <!-- Formulário -->
    <div class="form-container">
        <div class="form-card glass-panel">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                <div class="form-group">
                    <label asp-for="Titulo" class="form-label">Título da Meta</label>
                    <input asp-for="Titulo"
                           class="form-input"
                           placeholder="Ex: Viagem para Europa"
                           aria-describedby="tituloHelp" />
                    <span asp-validation-for="Titulo" class="form-error"></span>
                    <small id="tituloHelp" class="form-text">Dê um nome descritivo para sua meta</small>
                </div>

                <div class="form-group">
                    <label asp-for="ValorObjetivo" class="form-label">Valor Objetivo</label>
                    <div class="input-currency">
                        <span class="currency-symbol">R$</span>
                        <input asp-for="ValorObjetivo"
                               type="number"
                               step="0.01"
                               min="0.01"
                               class="form-input"
                               placeholder="0,00"
                               aria-describedby="valorHelp" />
                    </div>
                    <span asp-validation-for="ValorObjetivo" class="form-error"></span>
                    <small id="valorHelp" class="form-text">Quanto você precisa juntar?</small>
                </div>

                <div class="form-group">
                    <label asp-for="DataLimite" class="form-label">Data Limite (Opcional)</label>
                    <input asp-for="DataLimite"
                           type="date"
                           class="form-input"
                           aria-describedby="dataHelp" />
                    <span asp-validation-for="DataLimite" class="form-error"></span>
                    <small id="dataHelp" class="form-text">Até quando você quer alcançar essa meta?</small>
                </div>

                <div class="form-group">
                    <label asp-for="Descricao" class="form-label">Descrição (Opcional)</label>
                    <textarea asp-for="Descricao"
                              class="form-input"
                              rows="4"
                              placeholder="Descreva sua meta e o que ela significa para você..."
                              aria-describedby="descricaoHelp"></textarea>
                    <span asp-validation-for="Descricao" class="form-error"></span>
                    <small id="descricaoHelp" class="form-text">Adicione detalhes sobre sua meta</small>
                </div>

                <div class="form-divider"></div>

                <div class="form-actions">
                    <a asp-action="Index" class="btn-action btn-secondary">
                        <span class="material-symbols-outlined" aria-hidden="true">close</span>
                        Cancelar
                    </a>
                    <button type="submit" class="btn-action btn-success">
                        <span class="material-symbols-outlined" aria-hidden="true">check</span>
                        Criar Meta
                    </button>
                </div>
            </form>
        </div>
    </div>

</main>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="~/js/metasFinanceiras.js" asp-append-version="true"></script>
}